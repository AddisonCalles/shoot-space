import { EventListener } from "./eventListener.class";
export declare class Health {
    private _health;
    private _total;
    private _reduceEvent;
    private _deadEvent;
    constructor(total: number);
    reset(): void;
    reduce(reduce: number): void;
    get current(): number;
    get total(): number;
    get reduceEvent(): EventListener;
    get deadEvent(): EventListener;
    get isDead(): boolean;
}
